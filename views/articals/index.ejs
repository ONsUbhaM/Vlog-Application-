<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <title>Blog</title>
</head>
<body>
    <div class="container">
        <h1 class="mb-4 mt-2">Just Articals</h1>
        <a href="/articals/new" class="btn btn-success">New Artical</a>
        <% articals.forEach(artical => { %>
            <div class="card col p-4 d-flex flex-column positio n-static mt-4">
                <h3 class="mb-2"><%= artical.title %></h3>
                <div class="mb-2 date"><%= artical.createdAt.toLocaleDateString() %></div>
                <p class="card-text mb-2"><%= artical.description %></p>
                <div class="d-flex">
                <a href="articals/<%= artical.slug %>" class="btn btn-primary" style="margin-right: 10px;">Read More</a>
                <form action="/Articals/<%= artical.id %>?_method=DELETE" method="post">
                    <button class="btn btn-danger">Delete</button>
                </form>
                </div>
              </div>
        <% }); %>
        <%- include('footer') %>
    </div>
</body>
</html>